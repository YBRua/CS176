import { Shape } from "./mapGeneration";

// map general config
export const SIZE: Shape = { height: 10, width: 10 };
export const INITIAL_TIME = 5;

// bonus
export const NUM_BONUS = 10;
export const SCORE_PER_BONUS = 10;

// bomb
export const NUM_BOMB = 2;
export const PENALTY_PER_BOMB = -20;

// time
export const NUM_TIME = 1;
export const TIME_PER_TIME = 5;

// buff
export const NUM_BUFF = 1;
export const BUFF_MODIFIER = 2;
